# Agent rules — {{MODULE_NAME}}

## Scope

These rules apply to `{{MODULE_PATH}}`.

## Working agreements

- Read the nearest repository `AGENTS.md` and `.github/copilot-instructions.md` first.
- Preserve unrelated working-tree changes.
- Use `pwsh -NoProfile` for Windows PowerShell automation.
- Keep process contracts versioned and explicit; avoid silent fallbacks.
- Prefer targeted validation commands.
- Never commit credentials, tokens, private paths, or machine-specific secrets.